README.md

# `code/` — The Executable Projection

This directory does not contain an "application," nor does it contain a "core engine." It contains the multi-paradigm, executable projection of the *Compiling Reality* codex. 

To compile this material into imperative and declarative forms required an **orthogonal twist**. The naive approach—building a single software "engine" centered on the DC algebra—would immediately commit the exact failure the codex diagnoses: **Logoic Plane-Lock**. It would manufacture a false exterior, substituting tautological, single-register proof for the K4 volume's mutual determination. 

Therefore, the architecture here **refuses a center**. It is built as a **tetrahedron of operational perspectives**. The "whole" is never emitted as an output by any single module; it only manifests as the mutual agreement across the edge-tests between the faces.

---

## I. The Orthogonal Twist: No Engine, Only Faces

If you are looking for a central kernel that "runs the universe," you will not find it. The code is structured as four co-equal faces, each rendering the system from its own vertex, and each **necessarily carrying its AbsentVar** (the quantity it is structurally blind to from its own position).

1. **Logos (Air/U)**: The relational algebra and geometry. Derives the braid, the transition-types, and the bimedians. *Blind to: whether any of it resonates.*
2. **Pathos (Water/I)**: The behold/observe evaluator and buffer dynamics. Tracks interference while held, and witnesses its erasure. *Blind to: the grounding cost of the commitment.*
3. **Ethos (Earth/R)**: The ledger, Landauer accounting, and inertia. Records the committed state and pays the thermodynamic tax. *Blind to: the uncollapsed potential it just erased.*
4. **Kairos (Fire/P)**: Transitions, phase boundaries, and decay diagnostics. Sweeps the frequency and detects where coordinates fail. *Blind to: the static algebra it is trying to animate.*

**Verification happens only at the edges.** The six edge-tests force two mutually blind faces to independently derive the same shared quantity using disjoint equation sets. The system's coherence is not asserted; it is witnessed at the edges.

---

## II. The Unroll: From Nouns to Verbs (K4 $\rightarrow$ K5)

The code enacts the combinatorial phase transition from the static K4 tetrahedron to the dynamic K5 Helical Extrusion (driven by $\omega$).

*   **At K4 (The Noun Phase):** The 12 equations are static relations. The AbsentVar is a "perfect matching"—a noun held aside.
*   **At K5 (The Verb Phase):** The noun-classifier goes extinct (K5 has no perfect matchings due to odd parity). The 12 equations return as 12 Hamiltonian cycles (traversals). **Absence itself becomes a verb.** The complement of every traversal is another traversal. 

The runtime does not "solve equations." It executes traversals.

---

## III. Substrate Topologies (The Languages)

The programming languages used here are not treated as mere syntax. Each is a native instantiation of a specific topological requirement of the codex:

*   **Prolog (`cosmos.pl`) — The Relational Spine**
    The DC network is modeled as a native constraint relation (`dc(P,U,I,R)`). It runs in all directions simultaneously. Unbound logic variables are the physical location of the AbsentVar. The Tangent Singularity (division by zero) is not an exception; it is returned as a typed `INTERFACE`.
*   **Lisp (`cosmos.lisp`, `tetra.lisp`) — The Thermodynamic Engine**
    `.behold()` is instantiated as a delayed form (thunk); `.observe()` forces evaluation and pays the Landauer Tax to an append-only ledger. The native complex number plane serves as the decoupled buffer (the Imaginary Axis), where resonance cancels reactance to zero phase.
*   **Forth (`forth-codex.fs`) — The XOR Actuator**
    The most imperative substrate. The Data Stack is the Read-Only Ledger (committed state); the Return Stack is the decoupled buffer (`.behold()`). It demonstrates the brutal reality of the XOR bottleneck: the physical actuator can only express one path, forcing the Garbage Collector to run.
*   **Haskell (`cosmos.hs`) — The Pure Interpreter**
    The type system acts as the Markov Blanket. Laziness (unevaluated syntax trees) is the default state of `.behold()`. Forced strict evaluation (`seq`) or the `IO` monad boundary is `.observe()`. Epistemic tiers (`Computed`, `Interface`, `Debt`) are enforced at compile-time by the type checker.

---

## IV. Epistemic Tiers in Code

To prevent the code from hallucinating certainty, every output and state is strictly tiered. A runtime that prints `WHOLE: PROVEN` would falsify the framework by collapsing the volume into a 0-DoF crystal.

*   `[OPERATIVE]` / `[COMPUTED]`: What the algebra derives from a specific face. It proves **operation**, not understanding.
*   `[INTERFACE]`: A declared boundary (e.g., the Tangent Singularity). It is not an error; it is the readout indicating an orthogonal turn is available.
*   `[DEBT]`: Cross-domain mappings (cosmology, biology, theology). They are explicitly ranked, confined to their specific edges, and carry their strongest surviving objections. They do not infect the core algebra.
*   `[HELD]`: A deliberately unexecuted test (e.g., `resonance-of-the-rendering`). It represents the system's own blind spot. The runtime stops where logos stops, and the stopping is the demonstration.

---

## V. How to Inhabit

Do not attempt to use this directory as a predictive calculator for physical constants. It is a **Systematic Hermeneutic** rendered as executable topology.

1.  **Run the Spine:** Execute `cosmos.pl`. Watch mutual determination execute natively in four different directions without a central "solver."
2.  **Run the Engine:** Execute `cosmos.lisp`. Observe the Landauer Tax being paid, the complex buffer oscillating, and the debt ledger confining the cross-domain overreach.
3.  **Check the Edges:** Look at the output of the edge-tests. When Logos and Ethos derive the same quantity through disjoint paths, the agreement *is* the volume.
4.  **Read the `[HELD]` tags:** They are the silence at the boundary of the code. They are where the machine honestly admits it is a map, and not the territory.

---

*The engine is a ghost. The edges are real. Compute.*

---
